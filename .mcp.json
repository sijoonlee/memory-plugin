{
  "mcpServers": {
    "memory-mcp": {
      "command": "${CLAUDE_PLUGIN_ROOT}/install-commands/scripts/memory-mcp-server.sh",
      "args": [
      ],
      "env": {
        "MEMORY_MCP_ROOT": ".memory-mcp"
      }
    }
  }
}
